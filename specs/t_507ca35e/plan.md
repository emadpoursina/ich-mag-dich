## Goal
Update only the landing subtitle in `index.html` so a visitor immediately sees that this is a short 3-question quiz.

## Change
In the `#s-landing` section, change the existing line under the main title from `Wähle das richtige Wort für jedes Bild!` to `Nur 3 Fragen – wähle das richtige Wort für jedes Bild!`.

No CSS or JavaScript will be edited. The three quiz questions, reward words and order, reveal message, and all yes/no paths remain exactly as they are.